<UserControl
    x:Class="Famoser.BeerCompanion.Presentation.WinUniversal.UserControls.MainPage.LastBeers"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Famoser.BeerCompanion.Presentation.WinUniversal.UserControls.MainPage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    d:DataContext="{Binding MainPageViewModel.UserInformation.Beers, Source={StaticResource Locator}}">

    <UserControl.Resources>
        <DataTemplate x:Key="BeerTemplate" >
            <TextBlock TextAlignment="Center" Text="{Binding DrinkTime, Converter={StaticResource FormatDateTime}}" />
        </DataTemplate>
    </UserControl.Resources>
    
    <Grid>
        <ListView ItemsSource="{Binding }" ItemTemplate="{StaticResource BeerTemplate}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</UserControl>
